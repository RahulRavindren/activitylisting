<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DefaultSwitcherTheme" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="showText">false</item>
        <item name="android:switchMinWidth">50dp</item>
        <item name="android:trackTint">@color/yellow</item>
        <item name="android:thumbTint">@color/colorPrimaryDark</item>
    </style>

    <style name="CustomBottomNavigation" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">@android:color/black</item>
        <item name="itemIconTint">@color/bottom_icon_tint</item>
        <item name="itemTextColor">@color/bottom_icon_tint</item>
        <item name="android:textSize">12dp</item>
        <item name="android:padding">4dp</item>
    </style>

    <style name="YouFragmentTheme">
        <item name="android:background">@color/bottom_icon_tint</item>
        <item name="android:textColor">@color/light_gray_dark_shade</item>
    </style>

</resources>
